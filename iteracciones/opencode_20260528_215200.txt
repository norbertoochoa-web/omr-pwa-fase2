## RESUMEN SESIÓN - 28 MAYO 2026

### Proyecto: omr-pwa-fase2 (FastAPI + PostgreSQL + Docker)

### 1. Docker instalado y configurado
- Instalado Docker Engine + Docker Compose.
- Usuario `rodrigo` agregado al grupo `docker`.

### 2. Fixes aplicados al código
- **Dockerfile**: `libgl1-mesa-glx` → `libgl1` (paquete obsoleto en Debian trixie)
- **requirements.txt**: 
  - Agregado `bcrypt<5.0.0` (incompatibilidad passlib + bcrypt 5)
  - Agregado `rich>=13.0.0` (dependencia faltante)
- **app/omr_engine/processor.py**: Agregado `import cv2` faltante
- **app/routes/sessions.py**: Usar `selectinload(SessionModel.images)` para evitar error `MissingGreenlet` en async SQLAlchemy
- **app/routes/download.py**: Misma corrección + agregado `router = APIRouter()` que faltaba

### 3. API funcionando
- `http://localhost:8000/api/v1/health` → `{"status": "healthy"}`
- Seed ejecutado: usuario admin (`admin@test.com` / `password123`), template `imax_evaluacion`
- 9/10 tests pasan en `test_api.sh`. Test 6 falla: "No se detectaron marcas de esquina en cartilla4.jpeg"

### 4. Estructura creada
```
data/
└── catolico/
    ├── template.json      # Config cartilla 60 preguntas
    ├── evaluation.json    # Respuestas correctas
    ├── config.json        # Override procesamiento
    ├── omr_marker.jpg     # Patrón esquinas
    ├── cartilla4.jpeg     # Muestra prueba
    └── cartilla5.jpeg     # Muestra prueba
```

### 5. Pendientes para próxima sesión
- [ ] Modificar `seed.py` para leer template desde `data/catolico/` en vez de hardcodeado
- [ ] Agregar endpoint `POST /api/v1/templates` para subir config por institución
- [ ] Investigar por qué falla detección de marcas en cartilla4.jpeg (test 6)
- [ ] Agregar columna `institution_id` a tabla `templates` para multi-institución

### 6. Comandos importantes
```bash
# Iniciar contenedores
cd /home/rodrigo/Workspace/Imax/Mobil_web/omr-pwa-fase2
echo "manager" | sudo -S docker compose up -d --build

# Ver logs
echo "manager" | sudo -S docker compose logs api --tail 30

# Correr tests
bash test_api.sh

# Usar docker sin sudo (después de cerrar sesión y abrir de nuevo)
docker compose up -d
```
